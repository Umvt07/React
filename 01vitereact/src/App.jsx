import Icon from "./hello.jsx";
function App() {

  return (
    <>
    <Icon/>
    <h1>Adarsh Sharma</h1>
    </>
    // it returns a single element so we kept it inside a single element which is known as fragment-><> or it can also be inside any tag
  )
}
// {}-> this is treated as a variable which means it gives evaluated expression which means here we cant write java script code basically
// object wont able to compile the if-else things or anyother js concept
export default App
